.idea/
*.iml
.DS_Store
.vscode/

# machine-local overrides — never tracked. These hold the work identity, signing
# key paths and account-specific config; *.local covers .zshenv.local and
# .gitconfig.local without matching .gitconfig.local.template.
*.local
.aliases_company

# local audit reports — may quote findings verbatim
SECURITY-AUDIT.md
